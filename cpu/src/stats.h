#ifndef _STATS_H
#define _STATS_H

#include "cycle.h"

// Next functions manipulates integers
void swapping(ticks* a, ticks* b);
int partition(ticks arr[], int low, int high);
void quicksort(ticks arr[], int low, int high);

#endif